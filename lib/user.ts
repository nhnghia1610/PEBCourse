// types/user.ts
export interface CustomUser {
    id: string;
    firstName?: string;
    lastName?: string;
    fullName?: string;
    email?: string;
    // Add other properties based on your needs
  }
  